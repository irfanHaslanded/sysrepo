From: IrfanMohammad <irfan@graphiant.com>
Date: Fri, 11 Aug 2023 15:57:58 +0000
Subject: fix test failures and improve test infra

---
 tests/CMakeLists.txt       | 15 +++++++++------
 tests/tcommon.c            |  7 -------
 tests/test_massive_scale.c | 25 +++++++++++++++----------
 3 files changed, 24 insertions(+), 23 deletions(-)

diff --git a/tests/CMakeLists.txt b/tests/CMakeLists.txt
index e22fd75..8eddbe6 100644
--- a/tests/CMakeLists.txt
+++ b/tests/CMakeLists.txt
@@ -84,6 +84,9 @@ if(ENABLE_TESTS)
             "SYSREPO_SHM_PREFIX=_tests_sr_${test_name}"
             "SR_ENV_RUN_TESTS=1"
         )
+        if(${CMAKE_VERSION} VERSION_GREATER "3.7")
+            set_tests_properties(${test_name} PROPERTIES FIXTURES_REQUIRED tests_cleanup)
+        endif()
 
         # avoid loading installed plugins by sysrepo-plugind
         if(${test_name} STREQUAL "test_rotation")
@@ -92,9 +95,6 @@ if(ENABLE_TESTS)
             )
         endif()
 
-        if(${CMAKE_VERSION} VERSION_GREATER "3.7")
-            set_tests_properties(${test_name} PROPERTIES FIXTURES_REQUIRED tests_cleanup)
-        endif()
     endforeach()
     set(test_name test_sysrepocfg_oper_import)
     add_test(NAME ${test_name} COMMAND ${PROJECT_SOURCE_DIR}/tests/test_sysrepocfg_oper.sh WORKING_DIRECTORY ${CMAKE_BINARY_DIR})
@@ -103,6 +103,9 @@ if(ENABLE_TESTS)
         "SYSREPO_SHM_PREFIX=_tests_sr_${test_name}"
         "SR_ENV_RUN_TESTS=1"
     )
+    if(${CMAKE_VERSION} VERSION_GREATER "3.7")
+        set_tests_properties(${test_name} PROPERTIES FIXTURES_REQUIRED tests_cleanup)
+    endif()
 
 endif()
 
@@ -136,6 +139,9 @@ if(ENABLE_VALGRIND_TESTS)
             "SYSREPO_SHM_PREFIX=_tests_sr_${test_name}"
             "SR_ENV_RUN_TESTS=1"
         )
+        if(${CMAKE_VERSION} VERSION_GREATER "3.7")
+            set_tests_properties(${test_name} PROPERTIES FIXTURES_REQUIRED tests_cleanup)
+        endif()
 
         if(${CMAKE_VERSION} VERSION_GREATER "3.7")
             set_tests_properties(${test_name} PROPERTIES FIXTURES_REQUIRED tests_cleanup TIMEOUT 300)
@@ -148,9 +154,6 @@ if(ENABLE_VALGRIND_TESTS)
             )
         endif()
 
-        if(${CMAKE_VERSION} VERSION_GREATER "3.7")
-            set_tests_properties(${test_name} PROPERTIES FIXTURES_REQUIRED tests_cleanup)
-        endif()
     endforeach()
 endif()
 
diff --git a/tests/tcommon.c b/tests/tcommon.c
index 5ef6047..f261c59 100644
--- a/tests/tcommon.c
+++ b/tests/tcommon.c
@@ -27,11 +27,7 @@ _test_log_msg(sr_log_level_t level, const char *message, const char *prefix)
 {
     const char *severity = NULL;
     struct timespec ts = {0};
-    static int disable_debug = -1;
     static int enable_ts = 1;
-    if (disable_debug < 0) {
-        disable_debug = getenv("SR_TEST_LOG_DEBUG") == NULL;
-    }
 
     switch (level) {
     case SR_LL_ERR:
@@ -44,9 +40,6 @@ _test_log_msg(sr_log_level_t level, const char *message, const char *prefix)
         severity = "INF";
         break;
     case SR_LL_DBG:
-        if (disable_debug) {
-            return;
-        }
         severity = "DBG";
         break;
     case SR_LL_NONE:
diff --git a/tests/test_massive_scale.c b/tests/test_massive_scale.c
index c131448..8e6631b 100644
--- a/tests/test_massive_scale.c
+++ b/tests/test_massive_scale.c
@@ -87,6 +87,7 @@ setup_f(void **state)
         return 1;
     }
 
+    sr_disconnect(st->conn);
     clock_gettime(CLOCK_MONOTONIC, &start_time);
     return 0;
 }
@@ -96,13 +97,17 @@ teardown_f(void **state)
 {
     struct state *st = (struct state *)*state;
     const char *module_names[] = {
+        "test",
         "test-leafref",
         "test-module",
         "iana-if-type",
+        "ietf-if-aug",
         "ietf-interfaces",
-        "test",
         NULL
     };
+    if (sr_connect(0, &(st->conn)) != SR_ERR_OK) {
+        return 1;
+    }
 
     sr_remove_modules(st->conn, module_names, 0);
 
@@ -189,7 +194,7 @@ static void *
 test_massive_scale_helper(void *arg)
 {
     (void)arg;
-    const int TEST_RUN_SECONDS = test_full_scale ? 30 : 1;
+    const int TEST_RUN_SECONDS = test_full_scale ? 30 : 0;
     sr_subscr_options_t opts = SR_SUBSCR_ENABLED;
     int ret;
     cb_stats stats = {0};
@@ -331,13 +336,6 @@ test_multi_proc_scale(void **arg)
     int i, rc, ret, result = 0;
     pid_t pids[NUM_PROCS];
     ATOMIC_STORE_RELAXED(keep_running, 1);
-
-    ret = sr_connect(0, &conn);
-    assert_int_equal(ret, SR_ERR_OK);
-
-    ret = sr_session_start(conn, SR_DS_RUNNING, &sess);
-    assert_int_equal(ret, SR_ERR_OK);
-
     for (i = 0; i < NUM_PROCS; i++) {
         switch (pids[i] = fork()) {
             case 0:
@@ -350,6 +348,13 @@ test_multi_proc_scale(void **arg)
         }
     }
 
+    ret = sr_connect(0, &conn);
+    assert_int_equal(ret, SR_ERR_OK);
+
+    ret = sr_session_start(conn, SR_DS_RUNNING, &sess);
+    assert_int_equal(ret, SR_ERR_OK);
+
+
     for (i = 0; i < NUM_PROCS; i++) {
         rc = 0;
         wait(&rc);
@@ -367,7 +372,7 @@ test_multi_proc_scale(void **arg)
     assert_int_equal(result, 0);
 }
 
-static void
+void
 test_multi_thread_scale(void **arg)
 {
     const int NUM_THREADS = test_full_scale ? 8 : 1;
