From: IrfanMohammad <irfan@graphiant.com>
Date: Tue, 19 Sep 2023 00:12:12 +0000
Subject: rwlock with retry for better visibility

---
 src/common.c  | 74 ++++++++++++++++++++++++++++++++++++++++++++++++++++-------
 src/common.h  |  3 +++
 src/shm_sub.c | 17 ++++++++++++--
 3 files changed, 84 insertions(+), 10 deletions(-)

diff --git a/src/common.c b/src/common.c
index e8d6de0..2a6ab9c 100644
--- a/src/common.c
+++ b/src/common.c
@@ -147,6 +147,40 @@ const sr_module_ds_t sr_module_ds_disabled_run = {{
         SR_DEFAULT_NOTIFICATION_DS /**< notification */
     }};
 
+int
+sr_mutex_clocklock(sr_rwlock_t *rwlock, struct timespec *timeout_abs, sr_lock_mode_t mode, sr_cid_t cid, const char *func)
+{
+    int ret;
+    struct timespec now;
+    clock_gettime(COMPAT_CLOCK_ID, &now);
+
+    while (now.tv_sec + SR_COND_RETRY_STEP_SEC < timeout_abs->tv_sec) {
+        now.tv_sec +=  SR_COND_RETRY_STEP_SEC;
+        ret = pthread_mutex_clocklock(&rwlock->mutex, COMPAT_CLOCK_ID, &now);
+        if (!ret || ret != ETIMEDOUT) {
+            return ret;
+        }
+        SR_RWLOCK_WRN(rwlock, mode, cid, func, "delay mutex");
+    }
+
+    return pthread_mutex_clocklock(&rwlock->mutex, COMPAT_CLOCK_ID, timeout_abs);
+}
+
+int
+sr_cond_clockwait_retry(sr_cond_t *cond, pthread_mutex_t *mutex, struct timespec *timeout_abs)
+{
+    int ret;
+    struct timespec now;
+    clock_gettime(COMPAT_CLOCK_ID, &now);
+    if (now.tv_sec + SR_COND_RETRY_STEP_SEC >= timeout_abs->tv_sec) {
+        return sr_cond_clockwait(cond, mutex, COMPAT_CLOCK_ID, timeout_abs);
+    } else {
+        now.tv_sec +=  SR_COND_RETRY_STEP_SEC;
+        ret = sr_cond_clockwait(cond, mutex, COMPAT_CLOCK_ID, &now);
+        return ret == ETIMEDOUT ? EAGAIN : ret;
+    }
+}
+
 sr_error_info_t *
 sr_ptr_add(pthread_mutex_t *ptr_lock, void ***ptrs, uint32_t *ptr_count, void *add_ptr)
 {
@@ -2518,7 +2552,7 @@ sr_sub_rwlock(sr_rwlock_t *rwlock, struct timespec *timeout_abs, sr_lock_mode_t
     }
     if (!has_mutex) {
         /* MUTEX LOCK */
-        ret = pthread_mutex_clocklock(&rwlock->mutex, COMPAT_CLOCK_ID, timeout_abs);
+        ret = sr_mutex_clocklock(rwlock, timeout_abs, mode, cid, func);
     }
 
     SR_LOG_RWLOCK(rwlock, mode, cid, func, "Locking");
@@ -2546,7 +2580,11 @@ sr_sub_rwlock(sr_rwlock_t *rwlock, struct timespec *timeout_abs, sr_lock_mode_t
         ret = 0;
         while (!ret && (rwlock->readers[0] || rwlock->writer)) {
             /* COND WAIT */
-            ret = sr_cond_clockwait(&rwlock->cond, &rwlock->mutex, COMPAT_CLOCK_ID, timeout_abs);
+            ret = sr_cond_clockwait_retry(&rwlock->cond, &rwlock->mutex, timeout_abs);
+            if (ret == EAGAIN) {
+                SR_RWLOCK_WRN(rwlock, mode, cid, func, "delay locking");
+                ret = 0;
+            }
         }
         if (ret == ETIMEDOUT) {
             /* recover the lock again, the owner may have died while processing */
@@ -2584,7 +2622,11 @@ sr_sub_rwlock(sr_rwlock_t *rwlock, struct timespec *timeout_abs, sr_lock_mode_t
             }
 
             /* COND WAIT */
-            ret = sr_cond_clockwait(&rwlock->cond, &rwlock->mutex, COMPAT_CLOCK_ID, timeout_abs);
+            ret = sr_cond_clockwait_retry(&rwlock->cond, &rwlock->mutex, timeout_abs);
+            if (ret == EAGAIN) {
+                SR_RWLOCK_WRN(rwlock, mode, cid, func, "delay locking");
+                ret = 0;
+            }
         }
         if (ret == ETIMEDOUT) {
             /* recover the lock again, the owner may have died while processing */
@@ -2622,7 +2664,11 @@ sr_sub_rwlock(sr_rwlock_t *rwlock, struct timespec *timeout_abs, sr_lock_mode_t
         ret = 0;
         while (!ret && (rwlock->readers[SR_RWLOCK_READ_LIMIT - 1] || rwlock->upgr || rwlock->writer)) {
             /* COND WAIT */
-            ret = sr_cond_clockwait(&rwlock->cond, &rwlock->mutex, COMPAT_CLOCK_ID, timeout_abs);
+            ret = sr_cond_clockwait_retry(&rwlock->cond, &rwlock->mutex, timeout_abs);
+            if (ret == EAGAIN) {
+                SR_RWLOCK_WRN(rwlock, mode, cid, func, "delay locking");
+                ret = 0;
+            }
         }
         if (ret == ETIMEDOUT) {
             /* recover the lock again, the owner may have died while processing */
@@ -2659,7 +2705,11 @@ sr_sub_rwlock(sr_rwlock_t *rwlock, struct timespec *timeout_abs, sr_lock_mode_t
         ret = 0;
         while (!ret && (rwlock->readers[SR_RWLOCK_READ_LIMIT - 1] || rwlock->writer)) {
             /* COND WAIT */
-            ret = sr_cond_clockwait(&rwlock->cond, &rwlock->mutex, COMPAT_CLOCK_ID, timeout_abs);
+            ret = sr_cond_clockwait_retry(&rwlock->cond, &rwlock->mutex, timeout_abs);
+            if (ret == EAGAIN) {
+                SR_RWLOCK_WRN(rwlock, mode, cid, func, "delay locking");
+                ret = 0;
+            }
         }
         if (ret == ETIMEDOUT) {
             /* recover the lock again, the owner may have died while processing */
@@ -2785,7 +2835,11 @@ sr_rwrelock(sr_rwlock_t *rwlock, uint32_t timeout_ms, sr_lock_mode_t mode, sr_ci
         ret = 0;
         while (!ret && (rwlock->readers[1] || (rwlock->read_count[0] > 1) || rwlock->writer)) {
             /* COND WAIT */
-            ret = sr_cond_clockwait(&rwlock->cond, &rwlock->mutex, COMPAT_CLOCK_ID, &timeout_abs);
+            ret = sr_cond_clockwait_retry(&rwlock->cond, &rwlock->mutex, &timeout_abs);
+            if (ret == EAGAIN) {
+                SR_RWLOCK_WRN(rwlock, mode, cid, func, "delay relocking");
+                ret = 0;
+            }
         }
         if (ret == ETIMEDOUT) {
             sr_rwlock_recover(rwlock, func, cb, cb_data);
@@ -2847,7 +2901,11 @@ sr_rwrelock(sr_rwlock_t *rwlock, uint32_t timeout_ms, sr_lock_mode_t mode, sr_ci
             }
 
             /* COND WAIT */
-            ret = sr_cond_clockwait(&rwlock->cond, &rwlock->mutex, COMPAT_CLOCK_ID, &timeout_abs);
+            ret = sr_cond_clockwait_retry(&rwlock->cond, &rwlock->mutex, &timeout_abs);
+            if (ret == EAGAIN) {
+                SR_RWLOCK_WRN(rwlock, mode, cid, func, "delay relocking");
+                ret = 0;
+            }
         }
         if (ret == ETIMEDOUT) {
             sr_rwlock_recover(rwlock, func, cb, cb_data);
@@ -2960,7 +3018,7 @@ sr_rwunlock(sr_rwlock_t *rwlock, uint32_t timeout_ms, sr_lock_mode_t mode, sr_ci
         sr_timeouttime_get(&timeout_ts, timeout_ms);
 
         /* MUTEX LOCK */
-        ret = pthread_mutex_clocklock(&rwlock->mutex, COMPAT_CLOCK_ID, &timeout_ts);
+        ret = sr_mutex_clocklock(rwlock, &timeout_ts, mode, cid, func);
         if (ret == EOWNERDEAD) {
             /* make it consistent */
             ret = pthread_mutex_consistent(&rwlock->mutex);
diff --git a/src/common.h b/src/common.h
index 5ded494..5c9738f 100644
--- a/src/common.h
+++ b/src/common.h
@@ -1407,4 +1407,7 @@ void sr_rwlock_debug_set(sr_rwlock_t *rwlock, uint32_t enable);
 
 const char * sr_shm_dir_get(void);
 
+#define SR_COND_RETRY_STEP_SEC 3
+int sr_cond_clockwait_retry(sr_cond_t *cond, pthread_mutex_t *mutex, struct timespec *timeout_abs);
+
 #endif /* _COMMON_H */
diff --git a/src/shm_sub.c b/src/shm_sub.c
index 302ca9f..628b6e2 100644
--- a/src/shm_sub.c
+++ b/src/shm_sub.c
@@ -351,6 +351,7 @@ sr_shmsub_notify_new_wrlock(sr_sub_shm_t *sub_shm, const char *shm_name, sr_sub_
     sr_sub_event_t last_event;
     uint32_t last_request_id;
     int ret;
+    char buf[64] = "";
 
     /* it is only possible to lock with none or error */
     assert(!lock_event || (SR_SUB_EV_ERROR == lock_event));
@@ -375,7 +376,13 @@ sr_shmsub_notify_new_wrlock(sr_sub_shm_t *sub_shm, const char *shm_name, sr_sub_
     while (!ret && (sub_shm->lock.readers[0] || (ATOMIC_LOAD_RELAXED(sub_shm->event) &&
             (ATOMIC_LOAD_RELAXED(sub_shm->event) != lock_event)))) {
         /* COND WAIT */
-        ret = sr_cond_clockwait(&sub_shm->lock.cond, &sub_shm->lock.mutex, COMPAT_CLOCK_ID, &timeout_abs);
+        ret = sr_cond_clockwait_retry(&sub_shm->lock.cond, &sub_shm->lock.mutex, &timeout_abs);
+        if (ret == EAGAIN) {
+            snprintf(buf, sizeof(buf), "orig-CID %" PRIu32 " request ID %" PRIuFAST32,
+                    sub_shm->orig_cid, ATOMIC_LOAD_RELAXED(sub_shm->request_id));
+            SR_RWLOCK_WRN(&sub_shm->lock, SR_LOCK_NONE, cid, "delay new-wrlock", buf);
+            ret = 0;
+        }
     }
 
     last_event = ATOMIC_LOAD_RELAXED(sub_shm->event);
@@ -477,6 +484,7 @@ _sr_shmsub_notify_wait_wr(sr_sub_shm_t *sub_shm, sr_sub_event_t event, uint32_t
     uint32_t last_request_id, i, err_count;
     int ret, write_lock = 0;
     struct timespec timeout_abs2;
+    char buf[64] = "";
 
     assert((expected_ev == SR_SUB_EV_NONE) || SR_IS_NOTIFY_EVENT(expected_ev));
     assert(shm_data_sub->fd > -1);
@@ -492,7 +500,12 @@ _sr_shmsub_notify_wait_wr(sr_sub_shm_t *sub_shm, sr_sub_event_t event, uint32_t
     while (!ret && (sub_shm->lock.readers[0] || sub_shm->lock.writer ||
             (ATOMIC_LOAD_RELAXED(sub_shm->event) && !SR_IS_NOTIFY_EVENT(ATOMIC_LOAD_RELAXED(sub_shm->event))))) {
         /* COND WAIT */
-        ret = sr_cond_clockwait(&sub_shm->lock.cond, &sub_shm->lock.mutex, COMPAT_CLOCK_ID, timeout_abs);
+        ret = sr_cond_clockwait_retry(&sub_shm->lock.cond, &sub_shm->lock.mutex, timeout_abs);
+        if (ret == EAGAIN) {
+            snprintf(buf, sizeof(buf), "request ID %" PRIuFAST32, ATOMIC_LOAD_RELAXED(sub_shm->request_id));
+            SR_RWLOCK_WRN(&sub_shm->lock, SR_LOCK_NONE, cid, "delay wait", buf);
+            ret = 0;
+        }
     }
 
     /* we are holding the mutex but no lock flags are set */
